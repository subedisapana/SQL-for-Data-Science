/*
1. Create a database 'Classroom'
2. Create a table Science_class with these properties(
    Enrollment_No INT,
    Name VARCHAR,
    Science_Marks INT
)
*/

CREATE TABLE 'Science_class'(
    Enrollment_No INT,
    Name VARCHAR,
    Science_Marks INT
);
